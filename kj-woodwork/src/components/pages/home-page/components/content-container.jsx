import { Box, styled } from '@mui/material'; 

const ContentContainer = styled(Box)({
  position: 'absolute',
  top: 170,
  left: 0,
  height: '100%',
  width: '100%',
  zIndex: '2',
})

export default ContentContainer;
